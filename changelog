BETA NOTES

* fix lua error related to castbars when switching profiles with certain settings

* remove a no-longer-necessary update script, improving CPU usage whenever nameplates are visible
- (this is a relic of a hack I used to have to do to get pixel-perfectness working on nameplates)

* fix potential lua errors relating to GetNamePlateForUnit returning non-kui frames
* truncate overlong button string translations
* update zhTW

* "fix" lua errors if DBM is loaded but not initialised? or something?

* fix boss mod lines conflicting with MoveAnything

* update locale

* pull in changes from backport/2.22:
* reduce default bossmod line width, icon size, increase y offset slightly
* add bossmod_lines, bossmod_lines_width options
* fix boss mod icon position
* add boss mods line drawing capability

* config ui: remove scroll indicators because ugh

* correctly hide detached castbar's shadow when frame shadow is disabled
* fix lua error when both target arrows and castbar detach are enabled

* add options auras_on_friends, auras_on_enemies, auras_on_minus (not exposed in ui)
* add option auras_per_row (not exposed in ui)
* allow showing auras purge frame when "show auras" is disabled
* correctly propagate auras_highlight_other (not exposed in ui)
* correctly apply global scale to auras text size and offset options
* disable limit auras per row to 4 on minor units

* re-add full frame glow
* adjust default threat glow colours
* reduce full frame glow alpha
* config ui: add per-page copy/reset buttons
* config ui: design changes
